\ProvidesPackage{thuthesis}[2007/11/30 4.1 put any packages you would like to use here]

% 处理数学公式中的黑斜体的宏包
\RequirePackage{bm}

% 不同于 \mathcal \mathfrak 之类的英文花体字体
\RequirePackage{mathrsfs}

% 表格中支持跨行
\RequirePackage{multirow}

% 固定宽度的表格
\RequirePackage{tabularx}

% 表格中的反斜线
\RequirePackage{diagbox}

% 确定浮动对象的位置，可以使用 H，强制将浮动对象放到这里（可能效果很差）
\RequirePackage{float}

\usepackage{mathtools}

\usepackage{numprint}
\npthousandsep{\,}
\usepackage{fixltx2e}

\usepackage{forloop}

\usepackage{enumitem}
% \usepackage[resetfonts]{cmap}

\usepackage{cellspace}
\setlength\cellspacetoplimit{4pt}
\setlength\cellspacebottomlimit{4pt}
\newcommand\cincludegraphics[2][]{\raisebox{-0.4\height}{\includegraphics[#1]{#2}}}

%\setlist{parsep=0pt,listparindent=\parindent} 
%\setlist{  leftmargin=0pt, itemindent=20pt, labelwidth=15pt, labelsep=5pt, listparindent=\parindent, align=left}

% 浮动图形控制宏包。
% 允许上一个 section 的浮动图形出现在下一个 section 的开始部分
% 该宏包提供处理浮动对象的 \FloatBarrier 命令，使所有未处
% 理的浮动图形立即被处理。这三个宏包仅供参考，未必使用：
% \RequirePackage[below]{placeins}
% \RequirePackage{floatflt} % 图文混排用宏包
% \RequirePackage{rotating} % 图形和表格的控制旋转


\usepackage{colortbl}
\usepackage{arydshln}

% 给自定义的宏后面自动加空白
% \RequirePackage{xspace}

% 借用 ltxdoc 里面的几个命令。
\def\cmd#1{\cs{\expandafter\cmd@to@cs\string#1}}
\def\cmd@to@cs#1#2{\char\number`#2\relax}
\DeclareRobustCommand\cs[1]{\texttt{\char`\\#1}}

\newcommand*{\meta}[1]{{%
  \ensuremath{\langle}\rmfamily\itshape#1\/\ensuremath{\rangle}}}
\providecommand\marg[1]{%
  {\ttfamily\char`\{}\meta{#1}{\ttfamily\char`\}}}
\providecommand\oarg[1]{%
  {\ttfamily[}\meta{#1}{\ttfamily]}}
\providecommand\parg[1]{%
  {\ttfamily(}\meta{#1}{\ttfamily)}}
\providecommand\pkg[1]{{\sffamily#1}}

% 定义自己常用的东西


% \newcommand{\Norm}[2]{\left\( #1\;\delimsize\|\;#2 \right)}

\DeclarePairedDelimiterX{\infdivx}[2]{(}{)}{
  #1\;\delimsize\|\;#2
}



\DeclarePairedDelimiterX{\pardist}[2]{(}{)}{
  #1, #2
}

\newcommand{\NormDist}{\mathcal{N}}
\newcommand{\Loss}{\mathcal{L}}
\newcommand{\DKL}{D_{\mathrm{KL}}\infdivx}
\newcommand{\DJS}{D_{\mathrm{JS}}\infdivx}
\newcommand{\Div}{D\infdivx}

% \newcommand{\infdiv}{D\infdivx}
\newcommand{\DCD}{d_{\mathrm{CD}}\pardist}
\newcommand{\DEMD}{d_{\mathrm{EMD}}\pardist}
\newcommand{\DCDEMD}{d_{\mathrm{CD/EMD}}\pardist}

\newcommand{\MLP}{{\mathrm{mlp}}}

\newcommand{\PDATA}{p_{\mathrm {data}}}
\newcommand{\PMODEL}{p_{\mathrm {model}}}
\newcommand{\PROB}[1]{p_{\mathrm {#1}}}

\newcommand{\EXPECT}[2]{\mathbb{E}_{#1} {#2}}


% \newcommand{\X}{\bm x}
% \newcommand{\Z}{\bm z}
\newcommand{\argmin}[1]{\mathop{\arg\min}_{#1}\,}
\newcommand{\argmax}[1]{\mathop{\arg\max}_{#1}\,}





\DeclarePairedDelimiter{\Norm}{\lVert}{\rVert}

% \newcommand{\Norm}[1]{\left\| #1 \right\|}



\newcommand{\acite}{\textsuperscript}
% \newcommand{\threedrsns}{ 3D-R\textsuperscript{2}N\textsuperscript{2} }
\newcommand{\threedrsns}{ 3D-R{2}N{2} }




\newcolumntype{L}[1]{>{\raggedright\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}
\newcolumntype{C}[1]{>{\centering\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}
\newcolumntype{R}[1]{>{\raggedleft\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}


\endinput
